@model Edu.Application.QueryModel.ViewModels.Customers.CustomerIndexVM

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <thead>
        <tr>
            <th>FirstName</th>
            <th>LastName</th>
            <th>StreetAddress</th>
            <th></th>
        </tr>
    </thead>

    <tbody>

        @for (int i = 0; i < Model.Customers.Count(); i++)
        {
            var item = Model.Customers[i];
            <tr>
                <td>@Html.DisplayFor(c => item.FirstName)</td>
                <td>@Html.DisplayFor(c => item.LastName)</td>
                <td>@Html.DisplayFor(c => item.StreetAddress)</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
